"""
FLUJO HÍBRIDO IMPLEMENTADO:

1. Usuario completa FundApplication
   └── application.status = 'pending'
   └── application.accepts_terms_and_conditions = True

2. Admin revisa la aplicación
   └── application.status = 'approved'
   └── Se crea FundApproval

3a. Si NO hay cambios:
    └── approval.status = 'pre_approved'
    └── approval.terms_modified = False

3b. Si HAY cambios:
    └── approval.status = 'pre_approved_with_changes'
    └── approval.terms_modified = True
    └── approval.changes_summary = auto-generado

4. Sistema genera contrato final
   └── approval.status = 'awaiting_contract_signature'
   └── approval.final_contract_generated_at = now()
   └── approval.contract_signature_deadline = now() + 30 días

5. Usuario firma contrato
   └── approval.status = 'contract_signed'
   └── approval.final_contract_signed_at = now()

6. Sistema activa el contrato
   └── approval.status = 'active'
   └── Usuario puede proceder a invertir
"""